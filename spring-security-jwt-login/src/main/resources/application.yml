spring:
  datasource:
    url: jdbc:mysql://localhost:3306/jsblogs?createDatabaseIfNotExist=true
    username: root
    password: root

  jpa:
    hibernate:
      ddl-auto: update
logging:
  level:
    org.springframework.security: debug
